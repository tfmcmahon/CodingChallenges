
class CreditCard {

  constructor(firstName, lastName, cardNumber, month, year, type, ccv) {

    // Validate fields
    // Get bank by first 7 numbers

    _firstName = firstName
    _lastName = lastName
    _month = month
    _year = year
    _cardNumber = cardNumber
    _ccv = ccv
    _bank = /*bank, noted above*/
    _type = type

    this.updateInfo = function(/*...updates*/) {

      //Update the CC info
  
    }
  
    this.getInfo = function(/*...requests*/) {
  
      //e.g. return _name
  
    }

  }

}
